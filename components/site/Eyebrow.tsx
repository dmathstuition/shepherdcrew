export function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="mb-5 text-[11px] font-bold uppercase tracking-[0.4em] text-ember">
      {children}
    </p>
  );
}
