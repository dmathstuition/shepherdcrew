# Where /api/join forwards submissions.
# Use your Google Apps Script web app URL (deployed as "Anyone can access")
# or swap the route for a database insert once the portal exists.
GOOGLE_SCRIPT_URL=

# Public site URL, used for metadata and Open Graph
NEXT_PUBLIC_SITE_URL=https://theshepherdscrew.org
